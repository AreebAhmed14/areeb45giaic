"use strict";
let friends = ['Areeb', 'Ali', 'Bilal', 'hamza', 'alam'];
if (friends.length === 0) {
    console.log('we need to find to some friends.....');
}
else {
    friends = [];
    console.log('all friends have been removed ' + friends.length);
}
